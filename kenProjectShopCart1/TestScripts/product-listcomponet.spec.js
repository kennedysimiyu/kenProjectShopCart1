/*describe("The productList component", function () {
    beforeEach(module("kenApp"));

    var productsList;
    beforeEach(inject(function ($componentController) {
        productsList = $componentController("productsList", {
            $scope:{}
        });
    }));

    it("can be created", function () {
        expect(productsList).toBeDefined();
        expect(productsList.$onInit).toBeDefined();
    });
})*/